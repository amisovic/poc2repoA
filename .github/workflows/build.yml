name: Build

on:
  workflow_call:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    - name: Simulate build
      run: |
        ls -l
        echo "Building poc2repoA..."
        cat repoA.txt || true
        sleep 5
        echo "Build complete."